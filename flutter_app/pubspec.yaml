name: parking_app
description: Simple parking management app using Google Sheets

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  crypto: ^3.0.3
  fluttertoast: ^9.0.0
  share_plus: ^12.0.1
  path_provider: ^2.1.3

flutter:
  uses-material-design: true
